import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  errorContainer: {
    marginTop: 10,
  },
  errorText: {
    color: '#f00',
    fontSize: 12,
    lineHeight: 14,
  },
});
