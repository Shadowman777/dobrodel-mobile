import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  leftIcon: {
    color: '#fff',
    fontSize: 32,
    marginTop: 2,
    marginRight: 2,
  },
  closeIcon: {
    color: '#fff',
    fontSize: 32,
    alignSelf: 'flex-end',
  },
});
